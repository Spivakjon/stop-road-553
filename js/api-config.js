// ============================================================================
//  api-config.js  —  כתובת ה-API (השרת ב-Railway)
// ----------------------------------------------------------------------------
//  ⚠ זהו הקובץ היחיד שצריך לערוך כדי לחבר את האתר לשרת.
//  אחרי פריסת השרת ל-Railway, מעתיקים לכאן את הכתובת הציבורית שלו,
//  למשל: https://stop-road-553-api.up.railway.app  (בלי / בסוף).
//
//  כל עוד הערך ריק / "REPLACE_ME" — האתר עובד רגיל עם התוכן המובנה,
//  אבל ממשק הניהול וחיבור השרת מושבתים.
// ============================================================================

export const API_BASE = 'https://api-production-c3d7.up.railway.app';

export const API_ENABLED = typeof API_BASE === 'string'
  && API_BASE.length > 0
  && !API_BASE.includes('REPLACE_ME');
